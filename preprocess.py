import pandas as pd
import numpy as np
from pathlib import Path
from sklearn.preprocessing import StandardScaler
from sklearn.pipeline import Pipeline

from typing import Any

def drop_columnns(columns: list[str]) -> pd.DataFrame:
    pass

def transform_columns(numeric: list[str], categorical: list[str]) -> pd.DataFrame:
    pass

def encode_columns(columns: list[str]) -> pd.DataFrame:
    pass


def handle_mising(data: pd.DataFrame) -> pd.DataFrame:
    pass


